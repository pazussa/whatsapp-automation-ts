{
  "name": "qa-wapp-bdd-python",
  "version": "1.0.0",
  "description": "Guía rápida para ejecutar cada feature por nombre y/o archivo.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bdd:dry": "cucumber-js --dry-run",
    "cucumber": "cucumber-js",
    "bdd": "cucumber-js",
    "bdd:report": "bash -lc 'rm -rf reports && mkdir -p reports && cucumber-js -p report; status=$?; node scripts/generate-report.js || true; exit $status'",
    "bdd:report:render": "node scripts/generate-report.js",
    "bdd:cultivos": "bash -lc 'rm -rf reports && mkdir -p reports && cucumber-js -p cultivos; status=$?; node scripts/generate-report.js || true; exit $status'",
    "bdd:listar-cultivos": "bash -lc 'rm -rf reports && mkdir -p reports && cucumber-js -p listar-cultivos; status=$?; node scripts/generate-report.js || true; exit $status'",
    "bdd:fertilizante": "bash -lc 'rm -rf reports && mkdir -p reports && cucumber-js -p fertilizante; status=$?; node scripts/generate-report.js || true; exit $status'",
    "bdd:listar-fertilizantes": "bash -lc 'rm -rf reports && mkdir -p reports && cucumber-js -p listar-fertilizantes; status=$?; node scripts/generate-report.js || true; exit $status'",
    "bdd:fitosanitario": "bash -lc 'rm -rf reports && mkdir -p reports && cucumber-js -p fitosanitario; status=$?; node scripts/generate-report.js || true; exit $status'",
    "bdd:listar-fitosanitarios": "bash -lc 'rm -rf reports && mkdir -p reports && cucumber-js -p listar-fitosanitarios; status=$?; node scripts/generate-report.js || true; exit $status'",
    "bdd:consultar-campos": "bash -lc 'rm -rf reports && mkdir -p reports && cucumber-js -p consultar-campos; status=$?; node scripts/generate-report.js || true; exit $status'",
    "bdd:consultar-distribucion": "bash -lc 'rm -rf reports && mkdir -p reports && cucumber-js -p consultar-distribucion; status=$?; node scripts/generate-report.js || true; exit $status'",
    "bdd:temp-campana": "bash -lc 'rm -rf reports && mkdir -p reports && cucumber-js -p temp-campana; status=$?; node scripts/generate-report.js || true; exit $status'",
    "bdd:crear-campana": "bash -lc 'rm -rf reports && mkdir -p reports && cucumber-js -p crear-campana; status=$?; node scripts/generate-report.js || true; exit $status'",
    "bdd:consultar-trabajos": "bash -lc 'rm -rf reports && mkdir -p reports && cucumber-js -p consultar-trabajos; status=$?; node scripts/generate-report.js || true; exit $status'",
    "bdd:consultar-trabajos-hoy": "bash -lc 'rm -rf reports && mkdir -p reports && cucumber-js -p consultar-trabajos-hoy; status=$?; node scripts/generate-report.js || true; exit $status'",
    "bdd:asignar-precios-producto": "bash -lc 'rm -rf reports && mkdir -p reports && cucumber-js -p asignar-precios-producto; status=$?; node scripts/generate-report.js || true; exit $status'",
  "bdd:select": "node scripts/run-selected-features.js",
    "docs:plan": "node scripts/generate-internship-plan-doc.js",
    "docs:plan:html": "node scripts/docx-to-html.js",
    "docs:plan:preview": "node scripts/docx-to-html.js && xdg-open reports/plan-pasantia.html >/dev/null 2>&1 || true",
    "typecheck": "tsc --noEmit",
    "postinstall": "playwright install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "@cucumber/html-formatter": "^21.14.0",
    "@cucumber/pretty-formatter": "^2.1.0",
    "@types/node": "^24.3.0",
    "cucumber-html-formatter": "^3.1.0",
    "cucumber-html-reporter": "^7.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "docx": "^8.5.0",
    "dotenv": "^16.4.5",
    "mammoth": "^1.10.0",
    "playwright": "^1.55.0"
  }
}
